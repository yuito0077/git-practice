
Git / GitHub 基礎学習ノート (2026-06-09)

GitおよびGitHubの基本概念、ワークフロー、コマンド、そしてトラブルシューティングに関する知識をまとめた個人学習用ノートです。

1. GitとGitHubの全体像

概念をシンプルに整理すると以下のようになります。

ツール	役割	動作環境	主な用途
Git	バージョン管理システム（道具）	ローカル（自分のMacBook Airなど）	ファイルの変更履歴を記録・管理する
GitHub	ホスティングサービス（場所）	リモート（クラウド上のWebサーバー）	履歴をオンラインで保存し、他者と共有・共同開発する

2. 初期設定・確認コマンド

Gitを使用する前に、「誰が履歴を記録しているか」を識別するためのグローバル設定を行います。

[bash]
# Gitのバージョンを確認する
git --version

# ユーザー名とメールアドレスを登録する（メールアドレスはGitHubに登録したものと統一する）
git config --global user.name "yuito"
git config --global user.email "dev@yuito0077.com"

# 登録されている現在の設定を一覧表示して確認する
git config --global --list

# 【復習・修正ポイント】タイポなどで間違えて登録した不要な設定を削除する
git config --global --unset user.emaill


3. Gitの3つのエリアと基本ワークフロー

変更を記録する際、Git内には以下の**3つのエリア（スペース）**が存在します。これらを意識してコマンドを組み立てることが重要です。

 [ 1. ワークツリー ] ────( git add )────> [ 2. ステージングエリア ] ────( git commit )────> [ 3. ローカルリポジトリ ]
   (実際の作業フォルダ)                          (コミット対象の荷出し場)                        (履歴が永久保存される倉庫)


🛠️ ローカルでの基本ルーティン

1. 管理の開始（初回のみ）
現在のディレクトリをGitの管理下（リポジトリの初期化）にします。

[bash]
git init


2. 状態の確認（最重要コマンド）
現在どのファイルがどのエリアにあるか、変更が検知されているかなどを「泥臭く」確認します。

[bash]
git status


3. ステージング（荷出し）
ワークツリー（作業場）の変更を、次のコミット対象としてステージングエリアに載せます。

[bash]
# 特定のファイルを指定して載せる
git add readme.md

# ディレクトリ配下のすべての変更（新規作成、修正、削除）をまとめて載せる
git add .


4. コミット（倉庫への永久保存）
ステージングエリアに載せた変更に対して、分かりやすいメッセージを添えてローカルリポジトリに記録します。

[bash]
git commit -m "it commit test."


5. 履歴の確認
これまでローカルリポジトリに積み重ねてきたコミットの歴史をタイムライン順に確認します。

[bash]
git log


4. GitHubとの連携と共有（プッシュ）

手元（ローカル）で記録した歴史を、インターネット上の共有リポジトリ（リモート）へアップロードします。

[bash]
# リモートリポジトリのURLに「origin」というエイリアス（ニックネーム）をつけて登録する
git remote add origin [https://github.com/yuito0077/git-practice.git](https://github.com/yuito0077/git-practice.git)

# 登録されているリモートリポジトリのURLを確認する
git remote -v

# 【省略なしフルコマンド】指定したリモート（origin）のブランチ（main）へ送信する
git push origin main


> 💡 PRO TIPS

> • 初回プッシュ時の -u オプション: >   最初に git push -u origin main を実行しておくと、ローカルの main ブランチとリモートの main ブランチが強力に紐付きます（Upstream設定）。次回以降は git push または git pull と打つだけで、自動的に適切なブランチと同期が行えるようになります。

• 認証について: >   現在のGitHubはセキュリティの観点から「パスワード認証」が廃止されています。ターミナル上でパスワードを求められた場合は、GitHubの設定画面から生成した 「個人アクセストークン（PAT / ghp_... から始まる文字列）」 をパスワード欄に入力してください。

5. ブランチ（枝分かれ）の運用

本番環境や安定稼働している元の歴史（main）を壊さないように、安全に新機能の開発や実験用コードを書くための機能です。

[bash]
# ブランチの一覧を表示し、自分が今いる現在地（* がつく）を確認する
git branch

# 「test」という名前の新しいブランチを作成する（作成するのみで移動はしない）
git branch test

# 「test」ブランチに現在地を切り替える（移動する）
git checkout test
# ※最近のGitでは「git switch test」でも切り替え可能です


6. ブランチ名の変更

開発の状況に合わせて、ブランチ名を変更する必要が出てきた場合の対処法です。

【パターン1】変更したいブランチに「今自分がいる」場合

[bash]
git branch -m 新しいブランチ名


【パターン2】別のブランチ（mainなど）から「外側から変更する」場合

[bash]
git branch -m 古いブランチ名 新しいブランチ名


⚠️ 注意が必要な点（リモートとの同期問題） ローカルでブランチ名（例: test ‭$\rightarrow$‬ feature-a）を変更しても、GitHub側（リモート）のブランチ名は自動的には変わりません。完全に名前を一致させて綺麗にするには、以下の手順を踏む必要があります。

1. ローカルでブランチ名を変更する。

2. 新しい名前のブランチをリモートへプッシュする（git push origin 新しいブランチ名）。

3. GitHub上の古いブランチを削除する（git push origin --delete 古いブランチ名）。

⌨️ 【Linux/Vim共通】コミットメッセージを入れ忘れたときの対処法

コマンドラインで -m オプションを付けずに git commit を実行してしまった場合、ターミナル上でCUIテキストエディタの Vim (ヴィム) が自動的に起動します。焦らずに以下のステップで操作を完了させてください。

1. インサートモードに切り替える キーボードの i キーを1回押します。画面下部に -- INSERT --（または -- 挿入 --）と表示され、文字が入力できる状態になります。

2. メッセージを記述する ファイルの最上行に、コミットメッセージ（例: fix: add layout styling など）を記述します。

3. ノーマルモードに戻る 記述が終わったら、キーボード左上の Esc キーを1回押してインサートモードを抜けます（下部の表示が消えます）。

4. 保存して終了（コミット完了） 半角で :wq と入力し、Enter キーを押します（w は write = 保存、q は quit = 終了を意味します）。これでコミットが完了します。

	• ※保存せずに強制終了してコミット自体をキャンセルしたい場合は、:q! と入力して Enter を押してください。
